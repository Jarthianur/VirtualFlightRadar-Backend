/*test("checksum", [] {
    ASSERT_EQUALS(math::Checksum("", sizeof("")), 0);
    ASSERT_EQUALS(math::Checksum("\0", sizeof("\0")), 0);
    ASSERT_EQUALS(math::Checksum("$abc*", sizeof("$abc*")), 96);
}); test("checkLimits", [] {

    });*/
